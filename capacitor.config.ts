import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pt.ambrosio.reaproveitaapp',
  appName: 'Reaproveita',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
